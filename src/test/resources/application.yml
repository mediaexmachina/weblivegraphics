# Don't forget to update src/main/resources/META-INF/additional-spring-configuration-metadata.json
# after to adds non-official entries

spring:
    main:
        banner-mode: "off"
        log-startup-info: false
server:
    address: 127.0.0.1
    port: 8080

js:
    devmode: true

weblivegraphics:
    vendorProps: vendor/vendor.properties
    baseBackgroundFile: vendor/background.png
